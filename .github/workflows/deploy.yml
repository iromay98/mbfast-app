# ⚠ 廃止（2026-08-28）。デプロイは build-image.yml（build-and-deploy）に一本化。
# 旧方式（SSH→deploy-vps.sh のrsync）はVPSのGit管理と衝突し、実装が消える事故を
# 繰り返したため全廃した。このファイルはトリガーを持たない。
name: deploy-legacy-disabled
on:
  workflow_dispatch: {}
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "旧デプロイは廃止。build-and-deploy を使ってください"
