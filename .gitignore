# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# portable postgres data dir (local dev)
/.pgdata

# uploaded files (local storage driver) — 機微情報。公開・コミットしない
/storage

# generated prisma client
/src/generated

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local postgres data (belt & suspenders)
.pgdata/

# Claude Code local config
.claude/
.verify-out/
# prisma/data/wp-live/ … 価格表パーサーのオフライン検証フィクスチャ（LIVEスナップショット＋manifest）
#   をリポジトリで管理するため除外を解除（CIでラウンドトリップ検証を回す。公開価格の
#   マーケ用HTMLで秘密情報は含まない）。
prisma/data/airtable/
prisma/data/wp-backup/
public/ort/
public/models/plate-detect.onnx
